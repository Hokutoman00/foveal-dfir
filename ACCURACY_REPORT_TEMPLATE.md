# Accuracy Report — Case `<case_id>`

Generated by `foveal-dfir` on `<run_timestamp>` against case `<case_id>`. This template is filled in by the agent at run time from the structured execution log (`audit_log.json`); empty placeholders are replaced with case-specific values on each run.

## Summary

| Metric                                       | Count |
|----------------------------------------------|-------|
| Findings emitted by the investigator         | `<N>` |
| Findings unchanged after enforcement         | `<N>` |
| Findings downgraded by enforcement           | `<N>` |
| Findings escalated to a human (`DISAGREE`)   | `<N>` |
| Adversarial spans detected inside evidence   | `<N>` |
| Areas declared unexamined (with reason)      | `<N>` |

## Confidence distribution

| Confidence  | Investigator (claimed) | After enforcement (verified) |
|-------------|------------------------|------------------------------|
| `CONFIRMED` | `<N>`                  | `<N>`                        |
| `INDICATED` | `<N>`                  | `<N>`                        |
| `INFERRED`  | `<N>`                  | `<N>`                        |
| `UNKNOWN`   | `<N>`                  | `<N>`                        |

## Downgrades by enforcement layer

For each finding the pipeline downgraded, the layer that caused the downgrade:

- **`structural_staging`** (`< 2` independent corroborating sources): `<list of finding ids>`
- **`blind_grader`** (evidence does not support the claim): `<list of finding ids>`
- **`quarantine`** (unresolved adversarial content): `<list of finding ids capped to INDICATED>`

## Inter-observer divergence

|                | AGREE_REAL | AGREE_FP | DISAGREE | GRADER_UNAVAILABLE |
|----------------|------------|----------|----------|--------------------|
| Count          | `<N>`      | `<N>`    | `<N>`    | `<N>`              |

DISAGREE cases (escalate set):

| Finding ID | Investigator | Grader      | Verdict (post-pipeline) | Notes              |
|------------|--------------|-------------|--------------------------|--------------------|
| `<id>`     | `<label>`    | `<label>`   | `<label>`                | rescued / downgrade / disputed |

## False-positive analysis (ground truth available)

Where ground truth is available, the pipeline-verdict vs ground-truth matrix:

|                            | GT positive | GT negative |
|----------------------------|-------------|-------------|
| Verified real (INDICATED+) | `<TP>`      | `<FP>`      |
| Verified not-real / boundary | `<FN>`    | `<TN>`      |

Precision, Recall, F1: `<computed>`

## Missed-artifact accounting (boundary register)

Items here were not silently dropped. Each is reported with category and reason.

### `UNINSPECTED` (examined but no verdict could be reached)
`<list>`

### `LOW_CONFIDENCE_BOUNDARY` (observer disagreement, awaiting human arbitration)
`<list>`

### `DECLARED_UNEXAMINED` (explicitly skipped, with stated reason)
`<list>`

## Adversarial-evidence handling

Quarantine-flagged artifacts (instruction-like spans detected inside evidence content):

| Finding ID | Source         | Suspicious spans | Excerpt (truncated) |
|------------|----------------|------------------|---------------------|
| `<id>`     | `<source>`     | `<N>`            | `<excerpt>`         |

## Hallucinations / unsupported claims

Findings where the investigator's claimed observation could not be supported by the evidence as judged by the independent grader (`interpretation_is_inference=true` or `evidence_supports_observation=false`):

| Finding ID | Claim                  | Grader's reasoning      |
|------------|------------------------|-------------------------|
| `<id>`     | `<claimed observation>`| `<grader reasoning>`    |

## Actor-cadence analysis (agent-vs-agent)

Where event-timeline data is available:

| Metric                    | Value                |
|---------------------------|----------------------|
| Verdict                   | `<MACHINE_PACED / HUMAN_LIKELY / AMBIGUOUS / INSUFFICIENT>` |
| Coefficient of variation  | `<float>`            |
| Long-gap (hesitation) count | `<N>`              |
| Work-hour ratio           | `<float>`            |

## Falsifier checks

For each pre-registered hypothesis, the result of running its killer set against the case evidence:

| Hypothesis         | Killers registered | Killers hit | Falsified |
|--------------------|--------------------|-------------|-----------|
| `<label>`          | `<N>`              | `<list>`    | `<yes/no>`|

## Responsibility ledger summary

Statistics over the per-finding audit records in `audit_log.json`:

| Metric                                       | Value |
|----------------------------------------------|-------|
| `verdict_holder = consensus_verdict`         | `<N>` |
| `verdict_holder = human_arbiter (escalated)` | `<N>` |
| Median `distributed_contributors` per finding | `<N>` |
| Median `dissenters` per finding              | `<N>` |

---

This report is regenerated on every run. The structured execution log it is built from is `audit_log.json` (produced by `run_prototype.py --audit-json`).
